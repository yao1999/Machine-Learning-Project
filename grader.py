from sklearn.model_selection import cross_val_score
import numpy as np


# def get_score(predict, test, model_name):
#     # s = test.value
#     # count = 0
#     # for i in range(len(predict)):
#     #     if predict[i] == s[i]:
#     #         count+=1
    
#     # print(f'{model_name} Score: {str(count / len(predict))}')
#     pass